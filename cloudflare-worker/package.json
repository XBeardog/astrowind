{
  "name": "astrowind-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "wrangler d1 execute astrowind-db --file=./schema.sql",
    "init-db-local": "wrangler d1 execute astrowind-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251010.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.87.0"
  }
}
